from setuptools import setup, find_packages

setup(
    name='my_solar_monitor',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
    entry_points={
        'console_scripts': [
            'solar-monitor=my_solar_monitor.main:main',
        ],
    },
)
from setuptools import setup, find_packages

setup(
    name='my_solar_monitor',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
    entry_points={
        'console_scripts': [
            'solar-monitor=my_solar_monitor.main:main',
        ],
    },
)
from setuptools import setup, find_packages

setup(
    name='my_solar_monitor',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
    entry_points={
        'console_scripts': [
            'solar-monitor=my_solar_monitor.main:main',
        ],
    },
)
